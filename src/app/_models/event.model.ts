import {Location} from './location.model';
export class Event {
  location: Location[];
  max_voluntaries: number;
  id: number;
  name: string;
  description: string;
  duration: number;
  photos: string[];
  date: string;
  plus: string;
}
